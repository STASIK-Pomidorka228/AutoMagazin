spring:
  application:
    name: AutoMagazin

  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always

  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: redis

app:
  jwt:
    secret: "very_secret_key_very_secret_key_12345"
